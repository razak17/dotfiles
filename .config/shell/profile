#!/bin/zsh

typeset -U PATH path

LOCAL="$HOME/.local/bin"
ASDF_INSTALLS="$HOME/.asdf/installs"

# fzf
[ -f $HOME/.fzf.zsh ] && source $HOME/.fzf.zsh

# asdf
[ -f $HOME/.asdf/asdf.sh ] && source $HOME/.asdf/asdf.sh

# YARN
[ -d "$HOME/.yarn" ] && export YARNPATH="$HOME/.yarn/bin"

# fnm
if [ -e "$HOME/.fnm/fnm" ]; then
  version="v18.0.0"
  export FNMDIR="$HOME/.fnm"
  FNM_PREFIX="$HOME/.fnm/node-versions/$version/installation"
  export FNMPATH="$FNM_PREFIX/bin"
fi

# GO
if [ -d "$ASDF_INSTALLS/golang" ]; then
  v="1.18.1"
  export GOHOME="$ASDF_INSTALLS/golang/$v"
  export GOROOT="$ASDF_INSTALLS/golang/$v/go"
  export GOPATH="$HOME/go"
fi

# Azure cli
if [ -d "$HOME/lib/azure-cli" ]; then
  export AZCLI="$HOME/lib/azure-cli/bin"
fi

# Codelldb for use in neovim
local CODELLDB_DIR="$HOME/.vscode-oss/extensions/vadimcn.vscode-lldb-1.7.0"
if [ -d $CODELLDB ]; then
  export CODELLDB=$CODELLDB_DIR
fi

path=(
  "$LOCAL"
  "$LOCAL/dmenu"
  "$LOCAL/dwm"
  "$LOCAL/sb"
  "$LOCAL/statusbar"
  "$LOCAL/startup"
  "$LOCAL/utils"
  "$FNMDIR"
  "$GOHOME/packages/bin"
  "$GOPATH/bin"
  "$GOROOT/bin"
  "$FNMPATH"
  "$YARNPATH"
  "$AZCLI"
  "$path[@]"
)

export PATH
