#!/bin/sh

DIR="$HOME/pics/distro/main"
FAV="$HOME/pics/distro/main/cat_leaves.png"
wall=$(find "$DIR" -type f | shuf -n 1)

case $1 in
--fav)
	wall="$FAV"
	xwallpaper --zoom "$wall"
	;;
--pick)
	xwallpaper --zoom "$wall"
	;;
*)
	echo 'Must pass an option.'
	exit 1
	;;
esac
