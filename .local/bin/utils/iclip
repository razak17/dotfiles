#!/bin/sh

mkdir -p ~/.dots/clip
clip_history=~/.dots/clip/clip_hist.txt

while [ "$#" -gt 0 ]; do
	curr=$1
	shift

	case "$curr" in
    -s)
      echo "$(xclip -o -sel c)" >> $clip_history
      ;;
    -l)
      echo "$(cat $clip_history | dmenu -l 10)" | tr -n | xclip -sel c
      ;;
	esac
done
