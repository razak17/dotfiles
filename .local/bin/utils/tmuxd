#!/bin/bash -e

config_file="$HOME/.config/tmux/xd.tmux.conf"

if [[ -e $config_file ]]; then
  exec /usr/bin/tmux -L Dev -f "$config_file" new -s first -c$HOME/.config/nvim -n main
else
  echo config file does not exist
  exit 1
fi
