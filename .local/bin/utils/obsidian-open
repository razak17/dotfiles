#!/bin/sh

if [ "$TEXT_EDITOR" != 'rvim' ]; then
  notify-send "Only works when rvim is set as TEXT_EDITOR. Current TEXT_EDITOR is $TEXT_EDITOR"
  exit 1
fi

$TEXT_EDITOR -no-min -c "Obsidian search"
