#!/bin/bash

YAY="yay -Sy"
PAC="sudo pacman -S"

###############################################################
# NOTE:
# add this to /etc/pacman.conf after artix-archlinux-support is
# installed then run 'pacman -Syu'
# rc-update del lightdm default

# Arch
# [extra]
# Include = /etc/pacman.d/mirrorlist-arch

# [community]
# Include = /etc/pacman.d/mirrorlist-arch

# [universe]
# Server = https://universe.artixlinux.org/$arch
# Server = https://mirror1.artixlinux.org/universe/$arch
# Server = https://mirror.pascalpuffke.de/artix-universe/$arch
# Server = https://artixlinux.qontinuum.space:4443/artixlinux/universe/os/$arch
# Server = https://mirror1.cl.netactuate.com/artix/universe/$arch
##################################################################

# Install packages
pac_packages='exa plocate xorg-xinput wireless_tools github-cli fd htop ninja pamixer cmake make tmux ranger qbittorrent redshift mpv xclip brightnessctl alacritty lxappearance ripgrep maim lazygit ctags strace fd kitty bpytop screenkey python-pywal xwallpaper exa dunst axel elinks youtube-dl bat baobab bmon arc-gtk-theme pulseaudio gvfs xf86-input-libinput copyq android-tools android-file-transfer htop sloc lolcat pipewire pipewire-pulse pipewire-media-session'
# 'docker'
# 'postgresql'
# 'clang'
# 'firefox'
# 'rust-analyzer'
# 'timeshift'
# 'keepassxc'
# 'thunderbird'
# 'whatsapp-nativefier'
# 'telegram-desktop'
# 'vlc'
# 'brave'

yay_packages=(
	'autokey-gtk'
	'libxft-bgra'
	'zoxide'
	'bibata-cursor-theme'
	'devour'
	'toilet'
	'ttf-joypixels'
	'ueberzug'
	'jq'
	'ttf-ancient-fonts'
	# 'notion-app'
	# 'vscodium-bin'
	# 'skippy-xd'
	# 'packettracer'
	# 'wps-office'
)

installAurPackage() {
	echo 'Aur stuff'
	echo '\n'
	for i in "${yay_packages[@]}"; do
		echo Installing: $i
		$YAY $i
	done
}

installPacmanPackages() {
	$PAC $pac_packages
}

while [[ "$#" -gt 0 ]]; do
	curr=$1
	shift

	case "$curr" in
	-y | --yay)
		installAurPackage
		;;
	-pac | --pacman)
		installPacmanPackages
		;;
	-a | --all)
		installAurPackage
		installPacmanPackages
		;;
	*) echo "Unavailable command... $curr" ;;
	esac
done
